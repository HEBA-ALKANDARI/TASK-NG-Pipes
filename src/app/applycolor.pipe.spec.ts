import { ApplycolorPipe } from './applycolor.pipe';

describe('ApplycolorPipe', () => {
  it('create an instance', () => {
    const pipe = new ApplycolorPipe();
    expect(pipe).toBeTruthy();
  });
});
